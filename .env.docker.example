# =============================================================================
# ОБЯЗАТЕЛЬНЫЕ: Telegram
# =============================================================================
TELEGRAM_BOT_TOKEN=your_bot_token_here
TELEGRAM_BOT_USERNAME=your_bot_username

# =============================================================================
# ОБЯЗАТЕЛЬНЫЕ: Директория агента
# =============================================================================
# Директория внутри контейнера куда агент клонирует репозитории
APPROVED_DIRECTORY=/work

# =============================================================================
# АУТЕНТИФИКАЦИЯ CLAUDE (выбери один вариант)
# =============================================================================
# Вариант A (рекомендуется): OAuth-токен (claude setup-token, годовой)
# Получить: claude setup-token (на машине с авторизованным Claude Code)
# НЕ устанавливай ANTHROPIC_API_KEY если используешь OAUTH токен
CLAUDE_CODE_OAUTH_TOKEN=

# Вариант B: API ключ (для пользователей без подписки)
# ANTHROPIC_API_KEY=

# =============================================================================
# ДОСТУП (Telegram user IDs через запятую)
# =============================================================================
ALLOWED_USERS=

# =============================================================================
# ГОЛОСОВЫЕ СООБЩЕНИЯ (Groq-совместимый endpoint)
# =============================================================================
ENABLE_VOICE_MESSAGES=true
# Provider: openai (используем с Groq endpoint)
VOICE_PROVIDER=openai
OPENAI_API_KEY=your_groq_api_key_here
OPENAI_BASE_URL=https://api.groq.com/openai/v1
VOICE_TRANSCRIPTION_MODEL=whisper-large-v3-turbo

# =============================================================================
# РЕЖИМ PROJECT THREADS (группировка задач по проектам)
# =============================================================================
ENABLE_PROJECT_THREADS=false
# PROJECT_THREADS_MODE=private
# PROJECT_THREADS_CHAT_ID=
# PROJECTS_CONFIG_PATH=/app/config/projects.yaml

# =============================================================================
# DATABASE (хранилище сессий и тредов)
# =============================================================================
DATABASE_URL=sqlite:////data/bot.db

# =============================================================================
# MCP (Model Context Protocol)
# =============================================================================
ENABLE_MCP=false
# MCP_CONFIG_PATH=/app/config/mcp-config.json

# =============================================================================
# ЛОГИРОВАНИЕ
# =============================================================================
LOG_LEVEL=INFO

# =============================================================================
# ИНСТРУМЕНТЫ CLAUDE (whitelist)
# =============================================================================
# Оставить пустым для дефолтного набора или задать список через запятую
# CLAUDE_ALLOWED_TOOLS=

# =============================================================================
# HEARTBEAT (мониторинг polling-цикла)
# =============================================================================
# Файл обновляется ботом каждые ~30 секунд; healthcheck.sh проверяет его свежесть
HEARTBEAT_FILE=/data/heartbeat
